<div id="container">
  <h1><%= title %></h1>
  <p>Add ticks by pressing <span class="keys">Enter<span>.</p>
  <p>Save by by pressing <span class="keys">Tab</span>.</p>
  <p>Make urgent by adding <span class="red">**</span> to your Tick.</p>
  <p>Remove urgent state by adding <span class="red">*</span> to your Tick.</p>

  <section id="editable_container">
      <ol id="editable" contenteditable=true>
      <% for(var i=0; i<list.items.length; i++) { %>
              <li <% if(list.items[i].urgent) { %>data-urgent='true'<% } %>>
                  <%= list.items[i].task.replace(/^\s+|\s+$/g,"") %>
              </li>
          <% } %>
      </ol>
  </section>
</div>